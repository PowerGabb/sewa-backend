# Database Configuration - PostgreSQL Online (Koyeb)
spring.datasource.url=jdbc:postgresql://ep-blue-leaf-a2kgv5zf.eu-central-1.pg.koyeb.app:5432/koyebdb?sslmode=require&connectTimeout=30&socketTimeout=30
spring.datasource.username=koyeb-adm
spring.datasource.password=npg_KQVgG8oYOU7W
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Server Configuration
server.port=8080
server.servlet.context-path=/api

# JWT Configuration
jwt.secret=sewaKameraSecretKey2024VeryLongAndSecureKeyForJWTAuthentication256Bits
jwt.expiration=86400000

# Logging Configuration
logging.level.com.sewa=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN

# Allow circular references
spring.main.allow-circular-references=true

# Actuator Configuration
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
management.health.db.enabled=true

# Connection pool settings
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=90000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.initialization-fail-timeout=120000
spring.datasource.hikari.validation-timeout=10000
spring.datasource.hikari.leak-detection-threshold=60000